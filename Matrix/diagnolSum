// Matrix Diagonal Sum: Write a program to find the sum of the diagonals of a given square matrix. The input will be a square matrix, and the output should be the sum of its diagonals.

public class MatrixDiagonalSum {
    public static int diagonalSum(int[][] matrix) {
        int n = matrix.length;
        int diagonal1Sum = 0;
        int diagonal2Sum = 0;

        // Iterate through the rows of the matrix
        for (int i = 0; i < n; i++) {
            // Add the element at the current row and column to diagonal1Sum
            diagonal1Sum += matrix[i][i];
            // Add the element at the current row and the opposite column to diagonal2Sum
            diagonal2Sum += matrix[i][n - i - 1];
        }

        // Return the sum of the diagonals
        return diagonal1Sum + diagonal2Sum;
    }

    public static void main(String[] args) {
        int[][] matrix = {{1, 2, 3}, {4, 5, 6}, {7, 8, 9}};
        int result = diagonalSum(matrix);
        System.out.println("Sum of diagonals: " + result);
    }
}
